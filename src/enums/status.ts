export enum Status {
	SUCCESS = 'success',
	FAILURE = 'failure',
	ERROR = 'error',
	PENDING = 'pending',
	IN_PROGRESS = 'in_progress',
	CANCELLED = 'cancelled',
	TIMEOUT = 'timeout',
	SKIPPED = 'skipped',
	RETRY = 'retry',
	PARTIAL_SUCCESS = 'partial_SUCCESS',
	NONE = '',
}
